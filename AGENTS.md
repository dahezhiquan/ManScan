# AGENTS 约定

## 开发前阅读要求

在开始项目开发或修改代码前，必须先阅读项目根目录下的 `CHANGE.md`，用于了解项目的基本情况、历史变动和已有约定。

当需要进行复杂的架构设计时，还应同时参考项目根目录下的 `DESIGN.md`，以了解现有架构思路和设计约束。

## 变更记录要求

当生成的代码导致 `server` 目录之外的其他目录中的文件发生变动时，必须同步将本次变动的详细记录追加写入 `CHANGE.md`。

写入 `CHANGE.md` 时需要遵循以下格式：

## 当前时间 概要标题

正文需详细说明本次变动内容，至少应包含：

- 变动了哪些 `server` 目录之外的其他目录
- 这些目录下变动了哪些文件
- 每个文件的具体修改内容
- 这些修改的目的或影响

以下情况不属于这条记录要求的范围：

- 仅修改 `server` 目录下的文件
- 仅修改仓库当前根目录下的文件，例如根目录中的 `*.md`、配置文件或其他零散文件
