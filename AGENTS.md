# AGENTS 约定

## 开发前阅读要求

在开始项目开发或修改代码前，必须先阅读项目根目录下的 `CHANGE.md`，用于了解项目的基本情况、历史变动和已有约定。

在开发代码前，还必须阅读项目根目录下的 `CODE_STYLE.md`，并在整个开发、修改、自测和提交过程中严格遵循其中的开发规范、质量检查项与相关约束。

当需要进行复杂的架构设计时，还应同时参考项目根目录下的 `DESIGN.md`，以了解现有架构思路和设计约束。

## 代码规范要求

生成、修改或重构代码时，务必遵循项目根目录下 `CODE_STYLE.md` 中定义的开发规范。

执行要求如下：

- 不得违反 `CODE_STYLE.md` 中的功能、代码、性能、安全、测试、文档以及 `Server` 开发规范要求。
- 当 `CODE_STYLE.md` 与其他普通习惯或默认做法冲突时，以 `CODE_STYLE.md` 为准。
- 自检代码质量时，必须将 `CODE_STYLE.md` 作为主要检查清单。

## 前端问题排查要求

当需要判断问题是否可能来自前端实现时，可以直接查看前端项目代码辅助分析，前端项目路径为 `/Users/dahe/Documents/DAST/manscan-fe`。

执行要求如下：

- 允许读取、搜索和分析上述前端项目中的代码，用于定位问题来源或确认前后端边界。
- 禁止直接修改 `/Users/dahe/Documents/DAST/manscan-fe` 中的任何代码、配置或文档。
- 如确认问题位于前端，应在当前项目中明确说明判断依据和建议处理方向，而不是直接在前端项目中实施修改。

## 模版问题排查要求

当需要判断问题是否可能来自模版实现时，可以直接查看模版项目代码辅助分析，模版项目路径为 `/Users/dahe/Documents/DAST/manscan-templates`。

执行要求如下：

- 允许读取、搜索和分析上述模版项目中的代码或模版内容，用于定位问题来源、确认实现逻辑或判断与当前项目的边界。
- 禁止直接修改 `/Users/dahe/Documents/DAST/manscan-templates` 中的任何代码、模版、配置或文档。
- 如确认问题位于模版实现，应在当前项目中明确说明判断依据和建议处理方向，而不是直接在模版项目中实施修改。

## Server 接口文档要求

当且仅当 `server` 目录中的接口发生以下变化时，必须同步更新 `server/API.md` 文档：

- 新增接口
- 变更已有接口

这里的“接口变更”至少包括以下情况：

- 请求方法变更
- 请求路径变更
- 请求参数新增、删除、重命名、类型变更、是否必填变更
- 响应结构或字段含义变更
- 错误码新增、删除或语义变更
- 使用方式或调用示例需要随实现同步调整

执行要求如下：

- 该要求仅适用于 `server` 目录内的接口变更，不扩展到其他目录。
- 当接口代码已变更但 `server/api.md` 未同步更新时，视为本次开发未完成。
- 如果 `server/api.md` 不存在，则在本次接口变更中创建该文档；如果已存在，则在原文档基础上补充或更新对应接口说明。
- 文档内容必须与当前实际实现保持一致，不得沿用过期字段、路径或示例。

`server/api.md` 中每个接口至少必须说明以下内容：

- 请求方法和路径
- 请求参数（类型、是否必填、说明）
- 响应格式
- 错误码说明
- 使用示例

## 变更记录要求

当生成的代码导致 `server` 目录和 `docs` 目录之外的其他目录中的文件发生变动时，必须同步将本次变动的详细记录写入 `CHANGE.md`。

新增变更记录必须按照时间倒序写入到 `CHANGE.md` 文件最上方，确保最新记录排在最前面。

写入 `CHANGE.md` 时需要遵循以下格式：

## 当前时间 概要标题

正文需详细说明本次变动内容，至少应包含：

- 变动了哪些 `server` 目录和 `docs` 目录之外的其他目录
- 这些目录下变动了哪些文件
- 每个文件的具体修改内容
- 这些修改的目的或影响

以下情况不属于这条记录要求的范围：

- 仅修改 `server` 目录下的文件
- 仅修改 `docs` 目录下的文件
- 仅修改仓库当前根目录下的文件，例如根目录中的 `*.md`、配置文件或其他零散文件
