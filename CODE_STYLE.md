# 代码生成质量/格式检查清单

本文档用于开发、自测、联调和提交前的统一检查。目标不是增加流程负担，而是帮助我们在提交代码前尽早发现问题，保证功能质量、可维护性、性能、安全性与文档完整性。

## 功能检查

- 所有功能都能正常工作
  - 新增功能、修改过的功能、受影响的关联功能都应实际验证。
- 边界情况都处理了
  - 包括空值、超长输入、重复操作、极端参数、无权限、资源不存在等情况。
- 错误情况有友好提示
  - 错误信息应明确、可理解，避免仅返回模糊提示或直接暴露底层报错。
- 用户体验流畅
  - 页面跳转、加载状态、按钮交互、失败重试、表单提交等流程应自然顺畅，避免卡顿、阻塞或无反馈。

## 代码检查

- 代码符合项目规范
  - 保持与现有目录结构、编码风格、模块划分和工程约定一致。
- 变量和函数命名清晰
  - 命名应准确表达意图，避免使用含义模糊的缩写或临时命名。
- 没有重复代码
  - 相似逻辑应优先抽取为公共函数、组件或模块，避免复制粘贴式实现。
- 复杂逻辑有注释
  - 仅在逻辑不直观、约束较多或存在隐含前提时补充简洁注释，说明“为什么这么做”。
- 没有被注释掉的代码
  - 废弃逻辑应直接删除，不保留大段注释代码，以免影响可读性和维护性。

## 性能检查

- 没有不必要的重复计算
  - 避免在循环、渲染或高频调用中执行可缓存、可复用或可提前计算的逻辑。
- 列表渲染使用了 `key`
  - 前端列表渲染时应使用稳定且唯一的 `key`，避免使用不稳定索引导致渲染异常。
- 大数据量使用了分页或虚拟滚动
  - 列表、日志、表格等高数据量场景需要控制渲染成本和请求压力。
- 图片进行了优化
  - 包括压缩、裁剪、懒加载、尺寸适配、格式选择等，避免加载体积过大影响体验。

## 安全检查

- 用户输入都经过验证
  - 对输入内容做合法性、长度、格式和必要的白名单校验，避免异常数据进入系统。
- 敏感信息没有暴露
  - 不得在代码、日志、前端页面、接口响应或文档中暴露密码、令牌、密钥等敏感内容。
- API Key 使用环境变量
  - 凭证信息必须通过环境变量或安全配置注入，禁止硬编码到仓库中。
- 没有 SQL 注入风险
  - 数据库操作应使用参数化查询、预编译语句或安全 ORM 能力，避免拼接 SQL。

## 测试检查

- 核心功能有单元测试
  - 关键业务逻辑、公共方法、容易回归的模块应具备对应测试。
- 测试覆盖率达标
  - 以项目现有要求为准；若没有明确门槛，至少保证核心路径和关键分支被覆盖。
- 所有测试都通过
  - 提交前应确保与本次改动相关的自动化测试全部通过。
- 手动测试了主要流程
  - 对核心用户路径进行实际操作验证，尤其是自动化测试难以覆盖的交互链路。

## 文档检查

- README 是最新的
  - 功能说明、启动方式、依赖要求、使用示例应与当前实现保持一致。
- 重要函数有注释
  - 对关键入口、公共接口、复杂核心逻辑补充必要注释，提升维护效率。
- API 接口有文档
  - 接口用途、参数、返回值、错误码和使用限制应有清晰说明。
- 环境变量有说明
  - 需要的环境变量应写明名称、用途、默认值、是否必填及示例。

## Server 开发规范

本节仅适用于 `server` 目录下的服务端代码。`ManScan` 的服务端接口基于 Gin 框架开发，使用 GORM v2 作为 ORM，日志输出等操作优先复用 `ManScan` 现有日志能力。除非当前任务明确要求，否则生成或修改服务端代码时必须全部限定在 `server` 目录内，不要写入或改动其他目录。

### 技术栈约定

- Web 框架使用 Gin。
- ORM 使用 GORM v2。
- 日志优先复用 `ManScan` 已有日志封装，否则统一使用 Zap 结构化输出。
- 禁止使用 `fmt.Println`、`log.Println` 等方式进行调试输出。

### 目录结构约定

- `server/cmd/server`
  - 存放程序入口，例如 `main.go`。
- `server/config`
  - 存放配置文件和配置加载逻辑。
- `server/internal`
  - 存放核心业务代码。
- `server/internal/api`
  - 存放路由层代码。
- `server/internal/handler`
  - 存放控制器层代码。
- `server/internal/service`
  - 存放业务逻辑层代码。
- `server/internal/repository`
  - 存放数据访问层代码。
- `server/internal/model`
  - 存放数据模型层代码。
- `server/internal/model/entity`
  - 存放数据库实体定义，负责数据库字段映射。
- `server/internal/model/dto`
  - 存放请求和响应结构定义，负责 API 传输。
- `server/internal/middleware`
  - 存放中间件代码，例如 `Logger`、`Recovery`、`Cors`、`JWTAuth`、`RateLimit`。
- `server/internal/pkg`
  - 存放项目内部工具包，仅供服务端内部复用。
- 根目录 `pkg`
  - 用于存放可对外复用的通用工具包。

### 分层职责约定

- 项目采用经典分层架构，调用方向只能是 `Handler -> Service -> Repository`。
- 禁止反向调用，也禁止跨层调用。
- Handler 层只负责：
  - 接收 HTTP 请求。
  - 绑定参数。
  - 校验参数。
  - 调用 Service。
  - 返回统一响应。
- Handler 层绝对不允许：
  - 编写业务逻辑。
  - 直接访问数据库。
  - 直接操作事务。
- Service 层负责：
  - 所有业务逻辑。
  - 事务控制。
  - 多个 Repository 的编排。
  - DTO 与 Entity 的转换。
- Service 层只能依赖 `context.Context`，禁止依赖 `gin.Context`，以保证业务逻辑与 HTTP 框架解耦。
- Repository 层只负责数据库 CRUD 操作，不能包含业务规则。
- Model 层必须严格区分 DTO 和 Entity：
  - DTO 用于 API 传输，带 `binding` 校验 tag。
  - Entity 用于数据库映射，带 `gorm` tag。
  - DTO 与 Entity 的转换统一放在 Service 层完成。

### 接口与依赖注入约定

- Service 和 Repository 都必须先定义 interface，再写实现结构体。
- 接口名使用大驼峰命名，例如 `UserService`、`UserRepository`。
- 实现结构体使用小驼峰命名，例如 `userService`、`userRepository`。
- 构造函数统一命名为 `NewXxx`，并通过参数注入依赖。
- 禁止使用全局变量保存 `db`、`redis`、`logger` 等共享资源。
- 所有依赖必须在 `router.go` 或 `main.go` 中集中组装。

### Context 与数据库约定

- 所有 Service 和 Repository 方法的第一个参数必须是 `ctx context.Context`。
- 所有数据库操作都必须使用 `db.WithContext(ctx)`，以支持超时控制和链路追踪。
- Repository 层返回数据库原始错误，不在该层做业务语义转换。
- Service 层根据业务语义将底层错误转换为统一业务错误码或友好提示。

### 响应与错误处理约定

- 所有对外接口必须使用统一响应封装。
- 统一响应结构必须包含：
  - `code`
  - `message`
  - `data`
- 成功时统一调用 `response.Success(c, data)`。
- 失败时统一调用 `response.Fail(c, errcode, msg)`。
- 业务错误码统一定义在 `errcode` 包中。
- 禁止使用 `panic` 处理业务错误。
- 所有返回的 `error` 必须显式处理或继续向上返回，禁止忽略错误。

### 命名与文件约定

- 文件命名统一使用小写下划线格式，例如 `user_handler.go`。
- Handler 方法名使用动词命名，例如 `Create`、`Get`、`List`、`Update`、`Delete`。
- Repository 方法名遵循以下命名习惯：
  - `Create`
  - `FindByID`
  - `FindByXxx`
  - `Update`
  - `Delete`
  - `List`
- 公开方法必须配有简短注释，说明用途即可。

### 路由约定

- 路由采用 RESTful 风格。
- 路由必须按版本分组，例如 `/api/v1`。
- 所有路由统一集中在 `router.go` 中注册。
- 路由注册时按资源分组，保持清晰可读。

### 日志与中间件约定

- 日志统一使用 Zap 结构化输出。
- 关键业务操作必须记录日志，例如创建、修改、删除、登录等。
- 错误日志应尽量携带上下文信息，例如 `user_id`、`request_id`。
- 常用中间件统一放在 `server/internal/middleware` 目录下维护。

### 代码生成与新增功能顺序

- 生成代码时必须输出完整文件，包含 `package` 声明和完整 `import` 列表。
- 不要省略上下文，不要只输出局部片段。
- 新增一个业务功能时，按以下顺序生成对应代码：
  - `entity`
  - `dto`
  - `repository`
  - `service`
  - `handler`
  - `router`
- 每一层都应先定义接口，再写实现。
- 错误处理必须完整，不能忽略任何 `error` 返回值。

### Server 开发特别要求

- 尽量复用现有 `ManScan` 的函数和功能，避免重复造轮子。
- 在 `server` 中写代码时，务必将新增和修改内容限制在 `server` 目录内。
- 除非当前任务明确要求，否则不要修改 `server` 目录以外的源码、配置或文档。

